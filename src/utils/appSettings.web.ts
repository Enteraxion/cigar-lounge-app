/**
 * The browser half — see appSettings.ts. There is no app settings screen to
 * open, so screens must explain rather than offer a button.
 */
export const canOpenAppSettings = false;

export function openAppSettings(): void {
  // Nothing to open.
}
