<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Required to show a notification at all from Android 13 (API 33).

      It was reaching the merged manifest via firebase-messaging but never
      declared here and never requested, so it sat permanently denied:
      hasPermission() returned false, registerDeviceForPush() bailed out before
      asking FCM for a token, and no Android device ever registered. Push has
      been live since 2026-09-13 and worked on iOS the whole time, which is
      exactly why nobody noticed (2026-09-15).

      @react-native-firebase/messaging's requestPermission() asks for this on
      Android 13+, so the prompt comes from the same call the ID submission
      screen already makes on iOS.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
      Android 11 hid other installed apps from package queries by default, and
      SpeechRecognizer.isRecognitionAvailable() answers by looking for an app
      that serves RecognitionService. Without this declaration it returns false
      on a device that has one, and voice search would report itself
      unavailable on a working phone — the same wrong answer the iOS
      availability check gave in September, for a different reason.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">

      <!--
        Google Maps key for react-native-maps.

        Absent until 2026-09-15, which is why the Map tab fell back to
        SimplifiedMapView — pins on a blank grid, no streets, no geography.

        The value comes from MAPS_API_KEY in android/local.properties, which is
        gitignored. The key is restricted in Google Cloud Console to this
        package and to our debug and upload signing fingerprints, so it is only
        usable by builds we signed; that restriction, not secrecy, is what
        protects it. An unrestricted key embedded in an APK is somebody else's
        Maps bill.

        Empty string when the property is missing, so a checkout without it
        still builds and simply shows the simplified map.
      -->
      <meta-data
        android:name="com.google.android.geo.API_KEY"
        android:value="${MAPS_API_KEY}" />

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
